name: snakemake
channels: 
    - bioconda
    - conda-forge
dependencies: # unincluded packages with annoying dependencies: multiqc
    - snakemake>=7.22,<8 # likely works for versions 7.x.x, confirmed to work with 7.32.4
    - sra-tools
    - fastp
    - fastqc
    - bowtie2
    - star
    - methylpy
    - samtools
    - sambamba
    - bedtools
    - matplotlib # this and below is for python scripts
    - numpy
    - pandas
    - tqdm
    - pysam
    - genmap # there's one python script which uses this: generate_duplex_blacklists
